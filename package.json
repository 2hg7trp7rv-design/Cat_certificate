{
  "name": "tail-room",
  "private": true,
  "version": "0.2.0",
  "description": "A gentle digital pet game for people who cannot keep a cat or dog at home.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/app.js && node --check src/game-state.js && node --check src/pet-art.js && node --check src/icons.js && npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
