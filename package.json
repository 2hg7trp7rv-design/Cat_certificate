{
  "name": "tail-room-creator-preview",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "smoke": "npm run build && node scripts/browser-smoke.mjs"
  },
  "config": {
    "renderer": "webgl",
    "phaserVersion": "4.2.1"
  },
  "engines": {"node": ">=22"}
}
