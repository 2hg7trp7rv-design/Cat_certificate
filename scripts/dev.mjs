import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,normalize,resolve}from'node:path';const root=resolve(process.cwd()),port=Number(process.env.PORT||4173),mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.txt':'text/plain; charset=utf-8'};createServer(async(req,res)=>{try{const u=new URL(req.url||'/',`http://${req.headers.host||'localhost'}`);let p=decodeURIComponent(u.pathname);if(p==='/')p='/index.html';const full=normalize(join(root,p));if(!full.startsWith(root))throw Error();const info=await stat(full),file=info.isDirectory()?join(full,'index.html'):full,data=await readFile(file);res.writeHead(200,{'Content-Type':mime[extname(file)]||'application/octet-stream','Cache-Control':'no-store','X-Robots-Tag':'noindex, nofollow, noarchive'});res.end(data)}catch{res.writeHead(404,{'Content-Type':'text/plain; charset=utf-8'});res.end('Not found')}}).listen(port,'0.0.0.0',()=>console.log(`Tail Room creator preview: http://127.0.0.1:${port}`))
