    return `<section class="full-panel settings-panel panel-enter" role="dialog" aria-modal="true" aria-labelledby="settings-title">
      <header class="panel-header"><button data-close-panel class="icon-button icon-button--muted" aria-label="戻る">${icon('back',21)}</button><div><span class="section-kicker">SETTINGS</span><h1 id="settings-title">設定</h1></div><span class="panel-header__spacer"></span></header>
      <div class="settings-card">
        <label class="toggle-row"><span class="toggle-row__icon">${icon('volume',18)}</span><span><strong>音</strong><small>反応音・環境音</small></span><input id="sound-toggle" type="checkbox" ${this.state.soundEnabled ? 'checked' : ''}><i></i></label>
        <label class="toggle-row"><span class="toggle-row__icon">${icon('sparkles',18)}</span><span><strong>動きを少なくする</strong><small>アニメーションを抑える</small></span><input id="motion-toggle" type="checkbox" ${this.state.reduceMotion ? 'checked' : ''}><i></i></label>
      </div>
      <div class="settings-card routine-settings">
        <div class="setting-section-heading">${icon('clock',18)}<span><strong>生活時間</strong><small>現実の時計に合わせて暮らします</small></span></div>
        <div class="routine-grid routine-grid--settings">
          <label><span>起床</span><input id="routine-wake" type="time" value="${routine.wakeTime}"></label>
          <label><span>朝ごはん</span><input id="routine-breakfast" type="time" value="${routine.breakfastTime}"></label>
          <label><span>夜ごはん</span><input id="routine-dinner" type="time" value="${routine.dinnerTime}"></label>
          <label><span>就寝</span><input id="routine-bedtime" type="time" value="${routine.bedtime}"></label>
        </div>
      </div>
      <div class="settings-card">
        <button id="notification-button" class="setting-action-row"><span>${icon('bell',18)}</span><div><strong>空腹通知を確認</strong><small>Web確認版ではブラウザ通知を試します</small></div><em>${permission}</em></button>
        <div class="setting-static-row"><span>${icon('smartphone',18)}</span><div><strong>正式版の通知</strong><small>iOSではローカル通知として実装</small></div><em>後工程</em></div>
      </div>
      <div class="developer-card">
        <div class="developer-card__head"><div><span>DEVELOPER PREVIEW</span><strong>時間・成長確認</strong></div><em>${growth.label}</em></div>
        <p>${virtualLabel}<br><small>実際の端末時刻からの差：${Math.round(Number(this.state.debug?.timeOffsetMs || 0) / HOUR)}時間</small></p>
        <div class="developer-actions">
          <button data-advance-ms="${6 * HOUR}">+6時間</button>
