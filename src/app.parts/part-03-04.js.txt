    const themes = [['linen','リネン','sparkles'],['sage','セージ','leaf'],['night','夜の青','moon']]
    const cushions = [['round','まるクッション'],['cloud','雲のベッド'],['basket','籐のかご']]
    return `<section class="full-panel living-panel panel-enter" role="dialog" aria-modal="true" aria-labelledby="living-title"><header class="panel-header"><button data-close-panel class="icon-button icon-button--muted" aria-label="戻る">${icon('back',21)}</button><div><span class="section-kicker">LIVING</span><h1 id="living-title">ふたりの部屋</h1></div><span class="panel-header__spacer"></span></header><div class="living-intro"><div>${brandMark(true)}</div><p>少しずつ、${escapeHTML(this.state.pet.name)}が安心できる部屋にしていこう。</p></div><div class="setting-group"><div class="setting-group__title">${icon('palette',18)}<span><strong>部屋の空気</strong><small>光と色を選ぶ</small></span></div><div class="theme-choice-grid">${themes.map(([id,label,iconName]) => `<button data-theme="${id}" class="${room.theme === id ? 'is-selected' : ''}" aria-pressed="${room.theme === id}"><span class="theme-swatch theme-swatch--${id}">${icon(iconName,19)}</span><strong>${label}</strong>${room.theme === id ? icon('check',15) : ''}</button>`).join('')}</div></div><div class="setting-group"><div class="setting-group__title">${icon('sofa',18)}<span><strong>お気に入りの寝床</strong><small>見た目と寝る場所が変わる</small></span></div><div class="cushion-choice-grid">${cushions.map(([id,label]) => `<button data-cushion="${id}" class="${room.cushion === id ? 'is-selected' : ''}" aria-pressed="${room.cushion === id}"><span class="cushion-swatch cushion-swatch--${id}"></span><small>${label}</small></button>`).join('')}</div></div><label class="toggle-row"><span class="toggle-row__icon">${icon('flower',18)}</span><span><strong>観葉植物を置く</strong><small>部屋に小さな緑を追加</small></span><input id="plant-toggle" type="checkbox" ${room.plantVisible ? 'checked' : ''}><i></i></label></section>`
  }

  settingsHTML() {
    const routine = this.state.routine
    const now = this.now()
    const growth = getGrowthStage(this.state.pet, now)
    const virtualLabel = new Intl.DateTimeFormat('ja-JP', { year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' }).format(new Date(now))
    const permission = typeof Notification === 'undefined' ? '非対応' : Notification.permission === 'granted' ? '許可済み' : Notification.permission === 'denied' ? '拒否中' : '未設定'
